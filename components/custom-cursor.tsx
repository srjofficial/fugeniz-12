"use client"

// Custom cursor removed for performance — mix-blend-difference compositing
// was causing mouse lag on laptops. The native cursor is always better than
// a laggy custom one.

export default function CustomCursor() {
    return null
}
